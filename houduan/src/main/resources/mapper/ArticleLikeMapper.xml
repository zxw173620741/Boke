<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.demo.houduan.Mapper.ArticleLikeMapper">

    <insert id="insert">
        INSERT IGNORE INTO article_like (article_id, user_id, create_time)
        VALUES (#{articleId}, #{userId}, NOW())
    </insert>

    <delete id="delete">
        DELETE FROM article_like WHERE article_id = #{articleId} AND user_id = #{userId}
    </delete>

    <select id="countByArticleAndUser" resultType="int">
        SELECT count(1) FROM article_like
        WHERE article_id = #{articleId} AND user_id = #{userId}
    </select>
</mapper>